import { style } from "@vanilla-extract/css";

export const button = style({
  width: "30%",
  padding: "1rem",
  minWidth: "500px",
  textAlign: "left",
  cursor: "pointer",
});

export const buttonText = style({
  padding: "0.5rem",
});
